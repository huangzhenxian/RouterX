项目代号：RouteX

下面是完整的 Markdown 需求文档，你可以直接复制保存为：

```text
proxy-admin-system-requirements.md
```

---

# 代理管理系统需求文档

## 1. 项目概述

### 1.1 项目名称

代理管理系统（Proxy Admin System）

### 1.2 项目目标

基于 Xray-core 与 Go 构建一个高性能、高稳定性、多用户代理管理平台，实现：

* 用户管理
* 代理账号管理
* 流量统计
* 流量限制
* 节点管理
* 异常监控
* 出口代理管理
* 配置自动生成
* API 管理
* 日志审计

系统支持多节点部署，支持住宅代理出口，支持后期扩展订阅、套餐、工单等功能。

---

# 2. 技术架构

## 2.1 技术选型

| 模块   | 技术                  |
| ---- | ------------------- |
| 前端   | React 18 + TypeScript + Vite + Ant Design 5 |
| 服务端  | Go                  |
| 核心代理 | Xray-core           |
| 出口路由 | sing-box（可选）        |
| 数据库  | PostgreSQL / MySQL  |
| 缓存   | Redis               |
| 日志   | Zap                 |
| ORM  | GORM                |
| API  | Gin                 |
| 配置管理 | YAML / JSON         |
| 部署   | Docker Compose      |

---

# 3. 系统架构

```text
客户端
   ↓
Xray-core
   ↓
sing-box（可选）
   ↓
住宅 SOCKS5 / HTTP 代理
   ↓
目标网站
```

后台架构：

```text
Web 管理后台
       ↓
Go API 服务
       ↓
数据库 / Redis
       ↓
Xray API
       ↓
Xray-core
```

---

# 4. 功能模块

# 4.1 用户管理模块

## 功能列表

* 创建用户
* 编辑用户
* 删除用户
* 禁用用户
* 启用用户
* 用户套餐绑定
* 用户到期管理
* 用户流量限制
* 用户并发限制

## 用户字段

| 字段    | 类型       |
| ----- | -------- |
| ID    | bigint   |
| 用户名   | varchar  |
| UUID  | varchar  |
| 密码    | varchar  |
| 状态    | int      |
| 流量限制  | bigint   |
| 已使用流量 | bigint   |
| 到期时间  | datetime |
| 创建时间  | datetime |

---

# 4.2 节点管理模块

## 功能列表

* 节点添加
* 节点删除
* 节点状态监控
* 节点负载监控
* 节点在线状态
* 节点配置同步
* 节点重启
* 节点日志查看

## 节点字段

| 字段      | 类型      |
| ------- | ------- |
| ID      | bigint  |
| 节点名称    | varchar |
| IP      | varchar |
| 地区      | varchar |
| 状态      | int     |
| CPU 使用率 | float   |
| 内存使用率   | float   |
| 带宽      | bigint  |

---

# 4.3 代理协议模块

## 支持协议

* VLESS
* Trojan
* Shadowsocks
* VMess（可选）

## 推荐协议

### 推荐方案

```text
VLESS + Reality
Trojan + TLS
Shadowsocks 2022
```

---

# 4.4 流量统计模块

## 统计内容

* 用户上传流量
* 用户下载流量
* 节点总流量
* 每日流量
* 每月流量
* 当前连接数
* 历史流量统计

## 数据来源

通过 Xray Stats API 获取。

---

# 4.5 流量限制模块

## 功能

* 流量超额自动禁用
* 到期自动禁用
* 手动封禁
* 并发限制
* IP 限制（可选）

## 逻辑

```text
用户流量 >= 套餐流量
→ 自动禁用用户
→ 更新 Xray 配置
→ 通知客户端
```

---

# 4.6 出口代理模块

## 支持类型

* SOCKS5
* HTTP
* HTTPS

## 功能

* 住宅代理池
* 出口轮换
* 出口故障切换
* 出口延迟检测
* 出口区域管理

---

# 4.7 异常监控模块

## 监控内容

* Xray 进程状态
* 端口监听状态
* 节点在线状态
* 出口代理状态
* CPU 使用率
* 内存使用率
* 磁盘使用率
* 连接失败率
* 流量异常告警

## 告警方式

* Telegram
* 邮件
* Webhook

---

# 4.8 配置管理模块

## 功能

* 自动生成 Xray 配置
* 自动生成用户配置
* 自动生成订阅链接
* 自动同步节点配置
* 配置热更新

---

# 4.9 日志模块

## 日志分类

* 登录日志
* 操作日志
* 节点日志
* 系统日志
* 错误日志
* 流量日志

---

# 5. 数据库设计

# 5.1 users

```sql
CREATE TABLE users (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(64),
    uuid VARCHAR(128),
    password VARCHAR(128),
    status INT,
    traffic_limit BIGINT,
    used_traffic BIGINT,
    expire_time DATETIME,
    created_at DATETIME
);
```

---

# 5.2 nodes

```sql
CREATE TABLE nodes (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(64),
    ip VARCHAR(64),
    region VARCHAR(64),
    status INT,
    created_at DATETIME
);
```

---

# 5.3 user_traffic

```sql
CREATE TABLE user_traffic (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT,
    upload BIGINT,
    download BIGINT,
    created_at DATETIME
);
```

---

# 5.4 proxy_providers

```sql
CREATE TABLE proxy_providers (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    type VARCHAR(32),
    host VARCHAR(128),
    port INT,
    username VARCHAR(128),
    password VARCHAR(128),
    status INT
);
```

---

# 6. Go 服务模块设计

```text
/internal
    /api
    /service
    /repository
    /model
    /middleware
    /xray
    /monitor
    /scheduler
    /config
```

---

# 7. Xray 集成方案

## 7.1 管理方式

采用：

* Xray API
* 配置文件生成
* 动态 Reload

## 7.2 功能

* 动态增删用户
* 获取流量统计
* 获取连接状态
* Reload 配置

---

# 8. 部署方案

# 8.1 Docker Compose

组件：

```text
Go API
PostgreSQL
Redis
Xray-core
Prometheus
Grafana
Nginx
```

---

# 8.2 systemd

服务：

```text
proxy-admin.service
xray.service
sing-box.service
```

---

# 9. 服务器配置建议

# 9.1 开发环境

| 配置  | 推荐    |
| --- | ----- |
| CPU | 1 核   |
| 内存  | 1 GB  |
| 磁盘  | 20 GB |

---

# 9.2 小规模正式环境

适合：

```text
100 ~ 500 用户
```

推荐：

| 配置  | 推荐        |
| --- | --------- |
| CPU | 2 ~ 4 核   |
| 内存  | 4 GB      |
| 磁盘  | 40 GB SSD |
| 带宽  | 1 Gbps    |

---

# 9.3 中大型环境

适合：

```text
500 ~ 3000 用户
```

推荐：

| 配置  | 推荐         |
| --- | ---------- |
| CPU | 4 ~ 8 核    |
| 内存  | 8 ~ 16 GB  |
| 磁盘  | 100 GB SSD |
| 带宽  | 1 Gbps+    |

---

# 10. 后期扩展方向

## 可扩展功能

* 订阅系统
* 邀请系统
* 工单系统
* 余额充值
* Stripe 支付
* Telegram Bot
* 多租户
* API Key
* 节点自动扩容
* Kubernetes 部署

---

# 11. 安全设计

## 安全建议

* Reality 优先
* 禁止 VMess 明文
* API 鉴权
* JWT 登录
* Redis 限流
* Fail2Ban 防爆破
* TLS 全站 HTTPS
* 节点白名单

---

# 12. 推荐最终方案

## 推荐架构

```text
Go 管理后台
+
Xray-core
+
PostgreSQL
+
Redis
+
Prometheus
+
Grafana
```

## 推荐协议

```text
VLESS + Reality
```

## 推荐服务器配置

```text
2 核 CPU
4 GB 内存
40 GB SSD
1 Gbps 带宽
```

---

# 13. 项目阶段规划

## 第一阶段

* 用户管理
* Xray 集成
* 流量统计
* 节点管理

## 第二阶段

* 出口代理池
* 监控系统
* 自动告警

## 第三阶段

* 订阅系统
* 支付系统
* 多节点集群

---

# 14. 总结

本系统目标：

```text
高稳定性
高兼容性
高性能
易扩展
支持多用户
支持多节点
支持住宅代理出口
```

核心方案：

```text
Xray-core 负责用户入口
Go 服务负责业务逻辑
sing-box 负责复杂路由（可选）
```

最终形成完整代理管理平台。
